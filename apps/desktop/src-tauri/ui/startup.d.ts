/**
 * Declaration for the startup page's module.
 *
 * The page loads it as a module with a top-level await, so it has no exports;
 * this file exists so the page's test can import it for its effect.
 */

export {}
