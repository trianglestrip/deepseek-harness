/**
 * Declaration for the window initialization script the shell compiles in.
 *
 * The script defines `window.dsh` when a page loads, so the module has no
 * exports; this file exists so a TypeScript test can import it for its effect.
 */

export {}
